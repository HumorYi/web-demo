<!--
	1、样式优先级：
	* < 标签 < class < id < 行间样式
	2、.style的样式为行间样式；
	3、什么时候传参：函数里立定不下来的东西
	4、	oDiv.style.name = value;	//方法一
		oDiv.style[name] = value;	//方法二 js里面凡是用'.'的地方都能用[]代替，反之有时不行。['']=''：如果两边用的是变量或者是参数，两边就不需要添加引号,否则要添加引号
-->